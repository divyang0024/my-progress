-- use db;

-- create table

--     users(

--         uid integer not null primary key,

-- or

-- primary key(uid);

--         uname varchar(50)

--     );

-- insert into users (uid, uname)

-- values (78, 'ram'), (79, 'ram'), (75, 'ram'), (73, 'ram'), (74, 'ram');

-- select * from users;