// var gen_name = require('sillyname');
// basically now new version of node supports new version js syntax that involves writing 'import' instead of 'require' which is a much older way to import.
import gen_name from "sillyname"
var result = gen_name();
console.log(result);